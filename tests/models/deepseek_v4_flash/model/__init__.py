"""Declarative DeepSeek-V4-Flash model sources: ``@module`` classes at file
scope over a free ``config`` name (and, for cross-file nesting, other
injected names). Not importable on their own -- load with
``tests.models.loader.load_model``."""
